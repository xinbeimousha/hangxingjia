// 水平居中
x-middle(){
    display: flex;
    align-items: center;
}
// 垂直排列
y-view(){
    display: flex;
    flex-direction: column;

    height:100%;
}
// 固定到全屏
full-fixed(n){
    position:fixed;
    top:0;
    left:0;
    right:0;
    bottom:0;
    z-index:n;
}